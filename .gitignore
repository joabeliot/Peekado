.DS_Store

## Xcode
xcuserdata/
*.xcuserstate
*.xcuserdatad/
build/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

## Secrets — the Notion token lives in Keychain, never in the repo
*.secret
secrets.xcconfig

## lore — raw per-turn session transcripts (roll-up buffer, not for sharing)
lore/sessions/
